{
  "root": "libs/regex",
  "sourceRoot": "libs/regex/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/regex/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/regex"],
      "options": {
        "jestConfig": "libs/regex/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
